# src/runtime/

XRT host runtime wrappers live here.

Populated in **Phase 3**.

Build-system stub: `CMakeLists.txt` here intentionally fails when
`ACCEL_BUILD_XRT=ON` until the runtime layer exists.

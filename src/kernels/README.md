# src/kernels/

Vitis HLS kernel sources live here.

Populated in **Phase 2**.

Build-system stub: `CMakeLists.txt` here calls `add_accel_kernel(saxpy ...)`,
which intentionally fails with a clear "Phase 2 not implemented" error
until Phase 2 is implemented.

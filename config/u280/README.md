# config/u280/

Target configuration for u280.

This file gives Make/CMake a named target. You still need the matching Vitis
.xpfm platform installed at the path in `anvil.mk`, or override
`ANVIL_PLATFORM`/`ANVIL_VITIS_PLATFORM` for your local installation.

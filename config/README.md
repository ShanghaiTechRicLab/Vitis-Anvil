# config/

Platform-specific build and runtime configuration lives here.

Expected future contents include Vitis link configuration (`link.cfg`),
XRT runtime configuration (`xrt.ini`), and platform profile files.
Populated in **Phase 2/3/4**.
